# PediSim SVT Environment Configuration
# Copy this file to .env and fill in your values

# Optional: Enable AI-powered character responses
# When set, characters (Mark, Lily) will use Claude AI for dynamic responses
# When not set, characters use scripted fallback responses (still functional)
# VITE_ANTHROPIC_API_KEY=sk-ant-api03-...
